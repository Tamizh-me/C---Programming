#include<iostream>
#include<string>

using namespace std;
int main(){
    string a,b;
    cin>>a>>b;
    if(a==b){
        
      cout<<"Strings are same";
    }else{
        cout<<"Strings are not same";
    }
}